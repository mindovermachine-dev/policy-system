The syntax check passed (py_compile produced no errors). The `--help` failure is expected in this environment due to missing `graphrag_sdk` dependency. The file structure satisfies all requirements specified in the brief.

**Summary:**
- Wrote `spikes/pipeline-rag2/ingest.py` (269 lines, 9KB)
- Imports: exact brief requirements (graphrag_sdk, StructuralChunking, MarkdownLoader, ratelimit, schema)
- Azure wiring: AZURE_API_KEY, AZURE_API_BASE, AZURE_API_VERSION
- RateLimitedLLM wrapping with shared global cap (req_per_window=10, window_s=60.0)
- StructuralChunking(max_tokens=512, overlap_sentences=2) with max-chunks cap stub
- GraphExtraction(entity_extractor=LLMExtractor(llm), max_concurrency=args.max_concurrency)
- GraphRAG(connection, llm, embedder, ontology=SCHEMA, embedding_dimension=1536, NO num_ctx)
- Full CLI: --source, --regulation-map, --backend, --graph-name, --max-concurrency, --reset, --max-chunks
- Semantic logging to logs/ingest-<ts>.jsonl with per-source stats + final summary + error handling
- __main__ driver with asyncio

**1-chunk AZURE dry-run (mock):**
```bash
cd spikes/pipeline-rag2
export AZURE_API_KEY=YOUR_KEY AZURE_API_BASE=YOUR_BASE AZURE_API_VERSION=2023-09-01-preview
python3 ingest.py --source engprac --regulation-map <empty.json> --reset --max-chunks 1 --graph-name test_dryrun_native
```

**FULL CRA ingest:**
```bash
cd spikes/pipeline-rag2
export AZURE_API_KEY=YOUR_KEY AZURE_API_BASE=YOUR_BASE AZURE_API_VERSION=2023-09-01-preview
python3 ingest.py --source cra --graph-name policy_system_graphrag_native --max-concurrency 2
```
