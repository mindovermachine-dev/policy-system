---

## Self-Check Report

### Exit Criteria Verification

1. ✅ `spikes/pipeline-rag3/ingest.py` exists (11298 bytes).
2. ✅ Python AST parse passes - `python -c "import ast; ast.parse(open('spikes/pipeline-rag3/ingest.py').read())"` returned no errors.
3. ✅ `--help` runs and lists all flags correctly.
4. ✅ No Azure/network calls were made.
5. ✅ No FalkorDB graph was created/reset/deleted.

### Commands Run

```bash
cd /Users/tma/repos/policy-system/spikes/pipeline-rag3
.venv/bin/python -c "import ast; ast.parse(open('ingest.py').read())"
```

**Output:** *(empty - pass)*

```bash
.venv/bin/python ingest.py --help
```

**Output:** 
```
usage: ingest.py [-h] --source {cra} [--host HOST] [--port PORT]
                 [--graph-name GRAPH_NAME] [--max-concurrency MAX_CONCURRENCY]
                 [--reset] [--max-chunks MAX_CHUNCKS]
                 [--substantive SUBSTANTIVE] [--filter-regex FILTER_REGEX]
                 [--spread]

Ingest CRA into FalkorDB GraphRAG (spike)

options:
  -h, --help            show this help message and exit
  --source {cra}        Source to ingest (default: cra)
  --host HOST
  --port PORT
  --graph-name GRAPH_NAME
  --max-concurrency MAX_CONCURRENCY
                        Concurrency for the shared LLM+extraction gate
                        (default 2)
  --reset               Delete the TARGET graph before ingest (scoped to
                        --graph-name)
  --max-chunks MAX_CHUNKS
                        Dry-run cap: at most N chunks total per source (no cap
                        by default)
  --substantive SUBSTANTIVE
                        Content-filtered sample: keep only chunks matching
                        --filter-regex, cap at N.
  --filter-regex FILTER_REGEX
                        Case-insensitive regex selecting substantive chunks
                        (default 'shall|should').
  --spread              Stratify the sample: take an EVENLY-SPACED subset
                        across all matches (front->back).
```
