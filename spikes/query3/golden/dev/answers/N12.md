# N12 — expected routing (dev set, stress probe)

- source: query3/experiment_generalization_stress_test.py
- tuned_against: true (written AFTER the matchers, deliberately probing them)
- independent_recompute_needed: false

No golden answer exists for this question. It is a routing probe, not a graded question.

**Pre-registered expectation (written before the run):** fallthrough (tests query2's own H5 catalog regex, not just my new matchers)

Score by comparing the mechanism's routing stage against that expectation, not by grading an answer body.
