{{- if .Values.authentik.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "policy-system.fullname" . }}-authentik-postgres-data
  labels:
    {{- include "policy-system.labels" . | nindent 4 }}
    app.kubernetes.io/component: authentik-postgres
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.authentik.postgres.persistence.durableStorageClass.enabled }}
  storageClassName: {{ include "policy-system.authentikPostgresStorageClassName" . | quote }}
  {{- else if .Values.authentik.postgres.persistence.storageClassName }}
  storageClassName: {{ .Values.authentik.postgres.persistence.storageClassName | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.authentik.postgres.persistence.size | default "10Gi" }}
{{- end }}
